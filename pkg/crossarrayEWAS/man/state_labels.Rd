% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocks.R
\name{state_labels}
\alias{state_labels}
\title{Direction labels for the three block states}
\usage{
state_labels(neutral)
}
\arguments{
\item{neutral}{1-based column of the pinned state, i.e.
``fit_block_hsmm()$neutral``.}
}
\value{
character vector of length 3.
}
\description{
Labels the state columns given which one is pinned at mu = 0. When all
cluster effects fall on one side of zero the pinned state sorts to an end,
two states sit on the same side, and those get a column suffix rather than
being silently collapsed.
}
\details{
Notes carried over from the pipeline source:

Column labels for the three HSMM states, ordered by mu.

`neutral` is fit_block_hsmm()$neutral, the column pinned at mu = 0 (1-based
here). Normally that is the middle column and the labels are the familiar
hypo/neutral/hyper. When all cluster effects fall on one side of zero the
pinned state sorts to an end, so two states sit on the same side of no
change and a bare direction would be ambiguous; those get a column suffix
instead of being silently collapsed.
}
\examples{
state_labels(2)
state_labels(1)
}
