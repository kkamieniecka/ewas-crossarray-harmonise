% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg.R
\name{demean_rows_by_group}
\alias{demean_rows_by_group}
\title{Subtract group means from every probe of a methylation matrix}
\usage{
demean_rows_by_group(M, groups)
}
\arguments{
\item{M}{numeric matrix, probes x samples.}

\item{groups}{grouping vector of length ``ncol(M)``, normally subject
identifiers.}
}
\value{
numeric matrix of the same dimensions, each group's mean removed per
  row.
}
\description{
Row-wise (per probe) within-subject transform of a probes x samples matrix.
Uses a sparse averaging operator so cost stays linear in the number of
samples.
}
\details{
Notes carried over from the pipeline source:

Row-wise (per probe) subtraction of the group mean across samples.
}
\examples{
M <- matrix(as.double(1:8), nrow = 2)
demean_rows_by_group(M, groups = c("A", "A", "B", "B"))
}
