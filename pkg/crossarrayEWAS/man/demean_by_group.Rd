% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg.R
\name{demean_by_group}
\alias{demean_by_group}
\title{Subtract group means from a design matrix (the within transform)}
\usage{
demean_by_group(X, groups)
}
\arguments{
\item{X}{numeric matrix, samples x terms.}

\item{groups}{grouping vector of length ``nrow(X)``, normally subject
identifiers.}
}
\value{
numeric matrix of the same dimensions, each group's mean removed.
  Row names are carried over from ``X`` when it has them; when it does not,
  the returned matrix carries the integer group codes as row names, which is
  an artefact of the grouped-sum step rather than information about the
  samples.
}
\description{
Applies the within-subject transform to a samples x terms design matrix,
which is how subject intercepts are eliminated rather than estimated.
}
\details{
Notes carried over from the pipeline source:

Subtract the group mean from every column (the within transform).
}
\examples{
X <- cbind(exposure = c(0, 1, 0, 1), age = c(40, 41, 55, 56))
demean_by_group(X, groups = c("A", "A", "B", "B"))
}
